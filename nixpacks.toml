providers = ["php"]

[phases.build]
cmds = ["cd backend && composer install --no-dev --optimize-autoloader"]

[start]
cmd = "cd backend && php artisan config:clear && php artisan migrate --force && php artisan db:seed --force && php artisan serve --host=0.0.0.0 --port=$PORT"
